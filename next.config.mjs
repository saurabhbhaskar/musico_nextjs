/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'images.unsplash.com', 'res.cloudinary.com', 'images.pexels.com'],
      },
};

export default nextConfig;
